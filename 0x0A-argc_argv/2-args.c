#include <stdio.h>
#include "main.h"

/**
 * main - main function is the entry point
 * @argc: input prameter
 * @argv: input prameter
 * Return: always 0 (success)
*/
int main(int argc, char *argv[])
{
printf("%d", argc);
return (0);
}
