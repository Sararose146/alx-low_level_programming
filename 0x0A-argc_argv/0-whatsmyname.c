#include <stdio.h>


/**
 * main - main the enty point.
 * @argc: the number of command line arguments
 * @argv:is a prameter to contain the elements in array
 * Return: always 0 (success)
*/
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}

